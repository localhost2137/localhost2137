{
	"name": "@localhost2137/example-testing-parallel",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=24"
	},
	"scripts": {
		"test": "pnpm test:ownership && pnpm test:workers",
		"test:ownership": "vitest run --config vitest.ownership.config.ts",
		"test:workers": "vitest run --config vitest.config.ts",
		"typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
	},
	"devDependencies": {
		"hono": "4.13.4",
		"localhost2137": "workspace:*",
		"vitest": "4.1.11",
		"zod": "4.4.3"
	}
}
