# Getting started crash course

This executable example backs the documentation's first config and application call. It creates a
seeded Slack world, injects that instance's connection environment into a separate Node process,
and reads the world through Slack-shaped HTTP.

```sh
pnpm --filter @localhost2137/example-getting-started test
```
