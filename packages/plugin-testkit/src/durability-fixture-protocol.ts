/** Enables the selected plugin fixture's failure after durable time reconciliation. */
export const CONTRACT_FAIL_TIME_ADVANCE_ENV = "LOCALHOST2137_CONTRACT_FAIL_TIME_ADVANCE";

/** Prefix for one JSON-encoded `{ advanceId, from, to }` observation per reconciliation call. */
export const CONTRACT_TIME_ADVANCE_EVENT_PREFIX = "time-advance:";
